"""Shared helpers for VACS harmonization notebooks.

Import submodules directly (e.g. ``from utils.repo_paths import find_repo_root``)
so optional heavy dependencies are not loaded unless needed.
"""
